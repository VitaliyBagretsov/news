info:
  name: Delete media logo
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{backend_url}}/media/{{media_id}}/logo"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
