info:
  name: Get media by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{backend_url}}/media/{{media_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
